[project]
name = "prompt-translator"
version = "0.1.0"
description = "A thin terminal layer for translating prompts deterministically."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
    "pyyaml>=6.0",
]
